OPENAI_API_KEY=sk-...

# Point at the hand-written sample while the real pipeline isn't ready yet:
EVENTS_PATH=../sample_data/sample_events_for_dashboard_dev.json
MEDIA_DIR=../vision-pipeline/output_real

# Real data — all 7 Godrej pilot clips merged (run merge_real_events.py first):
# EVENTS_PATH=../vision-pipeline/output_real/merged_events.json
